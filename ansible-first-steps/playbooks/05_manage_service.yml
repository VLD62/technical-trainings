---
- name: Manage a service
  hosts: all
  become: true
  gather_facts: false

  vars:
    service_name: ssh

  tasks:
    - name: Ensure service is started and enabled
      ansible.builtin.service:
        name: "{{ service_name }}"
        state: started
        enabled: true
