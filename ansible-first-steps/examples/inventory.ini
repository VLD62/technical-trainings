# Replace these placeholder hosts with real lab hosts.
# For local testing, use:
# localhost ansible_connection=local

[local]
localhost ansible_connection=local

[web]
web01.example.local ansible_user=admin
web02.example.local ansible_user=admin

[db]
db01.example.local ansible_user=admin

[linux:children]
local
web
db
