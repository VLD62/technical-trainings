# Lab 5: CLI, Logging, and Configuration

## Tasks

1. Build a dry-run backup planning CLI.
2. Accept source, destination, include, exclude, and verbose options.
3. Recursively select matching files.
4. Calculate total count and size.
5. Use configuration precedence: CLI, environment, JSON, default.

## Stretch Task

Add JSON output mode.

## Review

- Which responsibility belongs in a function or class?
- Which errors are expected user errors?
- How will the behavior be tested?
