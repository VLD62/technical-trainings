# Provider-Free First Example

This example uses Terraform's built-in `terraform_data` resource and requires no external provider or cloud credentials.

```bash
terraform init
terraform fmt -check
terraform validate
terraform plan
terraform apply
terraform output
terraform state list
terraform destroy
```
