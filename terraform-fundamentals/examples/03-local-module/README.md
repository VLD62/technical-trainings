# Local Module Example

This example calls `modules/naming` and uses its output to create provider-free `terraform_data` resources.

```bash
terraform init
terraform validate
terraform plan
terraform apply
terraform output
terraform destroy
```
