# Variables and Collections

```bash
terraform init
terraform plan
terraform apply
terraform state list
terraform output
terraform destroy
```

Use the example variable file:

```bash
terraform plan -var-file=services.example.tfvars
```
